<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> | 5starjourney</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&amp;display=swap"
        rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />
    <link href="/css/styles.css" rel="stylesheet" />
    <script
        src="https://cdn.tailwindcss.com/3.4.5?plugins=forms@0.5.7,typography@0.5.13,aspect-ratio@0.4.2,container-queries@0.1.1"></script>
    <script src="https://ai-public.creatie.ai/gen_page/tailwind-config.min.js" data-color="#EB1F55"
        data-border-radius="medium"></script>
</head>

<body class="font-sans dark:bg-gray-900">

    <main class="max-w-8xl mx-auto px-6 py-16">
        <!-- 1. Bali image carousel and description -->
        <div class="max-w-8xl mx-auto px-6 py-16">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div class="md:col-span-2">
                    <div class="relative w-full h-96 mb-8">
                        <div class="carousel relative overflow-hidden rounded-xl">
                            <div class="flex transition-transform duration-500">
                                <img src="/assets/images/tours/bali-rice-terraces.jpg" class="w-full h-96 object-cover flex-shrink-0" alt="Bali View 1" />
                                <img src="/assets/images/tours/bali-temple.jpg" class="w-full h-96 object-cover flex-shrink-0" alt="Bali View 2" />
                                <img src="/assets/images/tours/bali-beach-sunset.jpg" class="w-full h-96 object-cover flex-shrink-0" alt="Bali View 3" />
                            </div>
                            <button class="absolute left-4 top-1/2 -translate-y-1/2 bg-white/80 hover:bg-white text-gray-800 p-2 rounded-full shadow-lg">
                                <i class="fas fa-chevron-left"></i>
                            </button>
                            <button class="absolute right-4 top-1/2 -translate-y-1/2 bg-white/80 hover:bg-white text-gray-800 p-2 rounded-full shadow-lg">
                                <i class="fas fa-chevron-right"></i>
                            </button>
                            <div class="absolute bottom-4 left-1/2 -translate-x-1/2 flex space-x-2">
                                <button class="w-2 h-2 rounded-full bg-white/60 hover:bg-white"></button>
                                <button class="w-2 h-2 rounded-full bg-white/60 hover:bg-white"></button>
                                <button class="w-2 h-2 rounded-full bg-white/60 hover:bg-white"></button>
                            </div>
                        </div>
                    </div>
                    <h1 class="text-4xl font-bold mb-4 dark:text-white"><%= tourName %></h1>
                    <div class="flex items-center space-x-4 mb-6">
                        <span class="bg-custom/10 text-custom px-3 py-1 rounded-full"><%= duration %> Days</span>
                        <span class="bg-custom/10 text-custom px-3 py-1 rounded-full"><%= tourType %></span>
                        <span class="bg-custom/10 text-custom px-3 py-1 rounded-full"><%= difficulty %></span>
                    </div>
                    <div class="prose dark:prose-invert max-w-none mb-8">
                        <h2 class="text-2xl font-semibold mb-4">Tour Overview</h2>
                        <p class="text-gray-600 dark:text-gray-400"><%= tourDescription %></p>
                        
                        <!-- 2. Highlights -->
                        <h2 class="text-2xl font-semibold mb-4 mt-8">Highlights</h2>
                        <ul class="list-disc pl-6 text-gray-600 dark:text-gray-400">
                            <% highlights.forEach(highlight => { %>
                                <li><%= highlight %></li>
                            <% }); %>
                        </ul>
                        
                        <!-- 3. Itinerary -->
                        <h2 class="text-2xl font-semibold mb-4 mt-8">Itinerary</h2>
                        <div class="space-y-4">
                            <% itinerary.forEach(day => { %>
                                <div class="border-l-4 border-custom pl-4">
                                    <h3 class="font-semibold"><%= day.title %></h3>
                                    <p class="text-gray-600 dark:text-gray-400"><%= day.description %></p>
                                </div>
                            <% }); %>
                        </div>
                    </div>
                </div>
                <div class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-lg h-fit sticky top-8">
                    <h3 class="text-2xl font-semibold mb-4 dark:text-white">Tour Details</h3>
                    <div class="space-y-4 mb-6">
                        <div class="flex justify-between">
                            <span class="text-gray-600 dark:text-gray-400">Duration:</span>
                            <span class="font-semibold dark:text-white"><%= duration %> Days</span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600 dark:text-gray-400">Group Size:</span>
                            <span class="font-semibold dark:text-white"><%= groupSize %> max</span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600 dark:text-gray-400">Difficulty:</span>
                            <span class="font-semibold dark:text-white"><%= difficulty %></span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600 dark:text-gray-400">Starting Point:</span>
                            <span class="font-semibold dark:text-white"><%= startingPoint %></span>
                        </div>
                    </div>
                    <button class="!rounded-button bg-custom text-white px-6 py-3 w-full mb-4">Book Now</button>
                    <button class="!rounded-button border-2 border-custom text-custom bg-transparent px-6 py-3 w-full">Download Brochure</button>
                </div>
            </div>
        </div>
        
        <!-- 4. Popular Attractions -->
        <section class="py-16">
            <div class="max-w-8xl mx-auto px-6">
                <h2 class="text-4xl font-bold text-center mb-12 dark:text-white">Popular Attractions</h2>
                <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
                    <% attractions.forEach(attraction => { %>
                        <div class="group rounded-xl overflow-hidden shadow-lg">
                            <div class="relative h-64 overflow-hidden">
                                <img src="/assets/images/attractions/<%= attraction.image %>" 
                                     class="w-full h-full object-cover transform group-hover:scale-110 transition-transform duration-500"
                                     alt="<%= attraction.name %>" />
                            </div>
                            <div class="p-6 bg-white dark:bg-gray-800">
                                <h3 class="text-xl font-semibold mb-2 dark:text-white"><%= attraction.name %></h3>
                                <p class="text-gray-600 dark:text-gray-400 mb-4"><%= attraction.description %></p>
                                <a href="#" class="text-custom hover:text-custom/80">Learn more →</a>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>
        </section>
        
        <!-- 5. Frequently Asked Questions -->
        <section class="py-16 bg-gray-50 dark:bg-gray-800 rounded-2xl">
            <div class="max-w-8xl mx-auto px-6">
                <h2 class="text-4xl font-bold text-center mb-12 dark:text-white">Frequently Asked Questions</h2>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <% faqs.forEach(faq => { %>
                        <div class="bg-white dark:bg-gray-700 rounded-xl p-6 cursor-pointer transition-all hover:shadow-lg">
                            <div class="flex justify-between items-center">
                                <h3 class="text-lg font-semibold dark:text-white"><%= faq.question %></h3>
                                <i class="fas fa-chevron-down text-custom transition-transform"></i>
                            </div>
                            <div class="mt-4 text-gray-600 dark:text-gray-300 hidden"><%= faq.answer %></div>
                        </div>
                    <% }); %>
                </div>
            </div>
        </section>
        
        <!-- Other Popular Tours section -->
        <section class="py-16 bg-gray-50 dark:bg-gray-800 rounded-2xl mt-10">
            <div class="max-w-8xl mx-auto px-6">
                <h2 class="text-4xl font-bold text-center mb-12 dark:text-white">Other Popular Tours</h2>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                    <% otherTours.forEach(tour => { %>
                        <div class="bg-white dark:bg-gray-700 rounded-xl overflow-hidden shadow-lg">
                            <img src="/assets/images/tours/<%= tour.image %>" class="w-full h-64 object-cover" alt="<%= tour.name %>" />
                            <div class="p-6">
                                <h3 class="text-xl font-semibold mb-2 dark:text-white"><%= tour.name %></h3>
                                <div class="flex items-center space-x-4 mb-4">
                                    <span class="bg-custom/10 text-custom px-3 py-1 rounded-full"><%= tour.duration %> Days</span>
                                    <span class="bg-custom/10 text-custom px-3 py-1 rounded-full"><%= tour.type %></span>
                                </div>
                                <p class="text-gray-600 dark:text-gray-300 mb-4"><%= tour.description %></p>
                                <div class="flex justify-between items-center">
                                    <span class="text-2xl font-bold text-custom">$<%= tour.price %></span>
                                    <a href="/tours/<%= tour.slug %>" class="text-custom hover:text-custom/80">View Details →</a>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                </div>
            </div>
        </section>
    </main>
    
    
    <script src="/js/carousel.js"></script>
</body>
</html>